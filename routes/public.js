// routes/public.js
const express = require('express');
const router = express.Router();
const pool = require('../db'); // Assuming you are using PostgreSQL with the pool setup for DB queries


module.exports = router;
