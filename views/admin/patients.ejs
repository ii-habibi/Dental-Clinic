<!-- views/patients.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Manage Patients</title>
</head>
<body>
    <h1>Patient Management</h1>

    <!-- Search Form -->
    <form action="/dashboard/patients/search" method="GET">
        <input type="text" name="query" placeholder="Search by name, email, or phone">
        <button type="submit">Search</button>
    </form>

    <!-- Add Patient Form -->
    <h2>Add New Patient</h2>
    <form action="/dashboard/patients/" method="POST">
        <input type="text" name="name" placeholder="Name" required>
        <input type="number" name="age" placeholder="Age" >
        <input type="text" name="gender" placeholder="Gender" >
        <input type="email" name="email" placeholder="Email" >
        <input type="text" name="phone" placeholder="Phone" required>
        <input type="text" name="address" placeholder="Address">
        <button type="submit">Add Patient</button>
    </form>

    <!-- List of Patients -->
    <h2>All Patients</h2>
    <table>
        <tr>
            <th>Name</th>
            <th>Age</th>
            <th>Gender</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Address</th>
            <th>Actions</th>
        </tr>
        <% patients.forEach(patient => { %>
            <tr id="">
                <td><%= patient.name %></td>
                <td><%= patient.age %></td>
                <td><%= patient.gender %></td>
                <td><%= patient.email %></td>
                <td><%= patient.phone %></td>
                <td><%= patient.address %></td>
                <td>
                    <a href="/dashboard/patients/edit/<%= patient.patient_id %>">Edit</a>
                    <a href="/dashboard/patients/delete/<%= patient.patient_id %>">Delete</a>
                </td>
            </tr>
        <% }) %>
    </table>
</body>
</html>
